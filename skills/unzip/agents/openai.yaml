interface:
  display_name: "unzip"
  short_description: "Extract and test ZIP archives safely"
  default_prompt: "Use $unzip to list, test, and extract ZIP archives safely with destination control and overwrite policies."
