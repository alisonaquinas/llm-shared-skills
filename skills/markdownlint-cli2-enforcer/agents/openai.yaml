interface:
  display_name: "Markdownlint CLI2 Enforcer"
  short_description: "Lint and fix Markdown with markdownlint-cli2"
  default_prompt: "Use markdownlint-cli2 with the configured jsonc file to lint or fix Markdown files, then report violations and changes."
