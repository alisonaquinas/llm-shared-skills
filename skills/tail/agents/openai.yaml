interface:
  display_name: "tail"
  short_description: "Sample file endings and monitor growing logs"
  default_prompt: "Use $tail to view file endings, follow live log updates with rotation support, and monitor append-only files."
