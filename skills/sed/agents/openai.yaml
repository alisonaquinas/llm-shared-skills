interface:
  display_name: "sed"
  short_description: "Stream-based text substitution and filtering with regex"
  default_prompt: "Use $sed to apply deterministic text transformations with regex patterns, line selection, and safe in-place editing with backups."
