interface:
  display_name: "diff"
  short_description: "Line-by-line file and directory comparison with patch generation"
  default_prompt: "Use $diff to compare files, detect changes, and generate unified patches for version control and review."
