interface:
  display_name: "ssh-keygen"
  short_description: "SSH key generation and management with fingerprinting"
  default_prompt: "Use $ssh-keygen to generate Ed25519 keys and manage SSH credentials securely."
