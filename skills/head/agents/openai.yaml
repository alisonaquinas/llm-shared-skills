interface:
  display_name: "head"
  short_description: "Sample file beginnings with line or byte limits"
  default_prompt: "Use $head to quickly preview the start of files, validate headers, and sample large files with explicit line or byte counts."
