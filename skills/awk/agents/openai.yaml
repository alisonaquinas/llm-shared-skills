interface:
  display_name: "awk"
  short_description: "Field extraction, filtering, and aggregation on structured text"
  default_prompt: "Use $awk to extract columns, filter rows, and compute aggregations on text data with proper field separators and pattern matching."
