interface:
  display_name: "binwalk Command"
  short_description: "Safe, reproducible workflows for binwalk"
  default_prompt: "Use $binwalk-command to plan and run reliable binwalk commands for this task."
