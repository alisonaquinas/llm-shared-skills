interface:
  display_name: "ar"
  short_description: "Manage static library archives with symbol table updates"
  default_prompt: "Use $ar to create and modify static library archives, rebuilding symbol tables for correct linkage."
